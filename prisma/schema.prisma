generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Report {
  id                 Int       @id @default(autoincrement())
  fazenda            String?   // opcional
  mesReferencia      String?   // opcional
  veterinario        String?   // opcional
  observacoes        String?
  taxaPrenhez        Int?
  taxaConcepcao      Int?
  taxaServico        Int?
  partosPrevistos    Int?
  inseminacoes       Int?
  diagnosticosPos    Int?
  matrizesExpostas   Int?
  createdAt          DateTime  @default(now())
}
